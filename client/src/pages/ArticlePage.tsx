/** Editorial Terminal: source-led long-form article layout with decision rails, cited product scope and clear related routes. */
import { ArrowUpRight, CheckCircle2, ChevronRight, ExternalLink, FileCheck2 } from "lucide-react";
import { Link } from "wouter";
import PageMeta from "@/components/PageMeta";
import SiteFooter from "@/components/SiteFooter";
import SiteHeader from "@/components/SiteHeader";
import { verifiedArticles } from "@/lib/site-data";

export default function ArticlePage({ slug }: { slug: string }) {
  const article = verifiedArticles.find((item) => item.slug === slug) ?? verifiedArticles[0];
  const hubPath = article.kind === "Review" ? "/reviews/" : "/comparisons/";
  const hubLabel = article.kind === "Review" ? "Software Reviews" : "Software Comparisons";
  return <div className="site-shell page-shell"><PageMeta title={article.title} path={article.path} description={article.description} /><SiteHeader /><main id="main-content"><section className="article-hero"><div className="breadcrumb"><Link href="/">Home</Link><ChevronRight size={14} /><Link href={hubPath}>{hubLabel}</Link><ChevronRight size={14} /><span>{article.title}</span></div><div className="article-signal"><span>{article.kind.toUpperCase()} / VERIFIED</span><i /><span>Source-led guide</span></div><span className="eyebrow"><FileCheck2 size={15} /> Checked against official product pages</span><h1>{article.title}<em>{article.emphasis}</em></h1><p>{article.description}</p><div className="article-meta"><span>Decision framework</span><span>Sources checked 21 August 2026</span><span>Plan details require verification</span></div><div className={`article-specimen ${article.kind === "Review" ? "article-review-specimen" : "article-comparison-specimen"}`} aria-label="Decision specimen"><div className="article-specimen-top"><span>DECISION PANEL / 01</span><span>{article.scopes.length} OFFICIAL {article.scopes.length === 1 ? "SOURCE" : "SOURCES"}</span></div><div className="article-specimen-body">{article.kind === "Review" ? <><div><span>01 / SCOPE</span><b>What the provider says</b></div><i /><div><span>02 / FIT</span><b>What your workflow needs</b></div></> : <><div><span>A / OPTION</span><b>State the workflow</b></div><i /><div><span>B / OPTION</span><b>Apply the same checks</b></div></>}</div></div></section><section className="article-layout"><aside className="article-rail"><span>ON THIS PAGE</span><a href="#scope">Official scope</a><a href="#investigate">Who should investigate</a><a href="#checks">Questions to check</a><a href="#context">Context to confirm</a><a href="#sources">Sources</a></aside><article className="article-body"><p className="article-lede">{article.intro}</p><section id="scope"><div className="body-label"><span>01</span><b>Official scope</b></div><h2>What the providers <em>say their products cover.</em></h2><div className="scope-stack">{article.scopes.map((scope) => <div className="scope-card" key={scope.product}><div><span>{scope.product}</span><a href={scope.url} target="_blank" rel="noreferrer">Official source [{scope.source}] <ExternalLink size={13} /></a></div><p>{scope.text}</p></div>)}</div></section><section id="investigate"><div className="body-label"><span>02</span><b>Possible fit</b></div><h2>Who should <em>investigate further.</em></h2><div className="check-grid">{article.investigate.map((item) => <p key={item}><CheckCircle2 size={17} />{item}</p>)}</div></section><section id="checks"><div className="body-label"><span>03</span><b>Trial questions</b></div><h2>Questions worth <em>checking before a decision.</em></h2><ol className="question-list">{article.checks.map((item, index) => <li key={item}><span>0{index + 1}</span><p>{item}</p></li>)}</ol></section><section id="context" className="context-note"><div className="body-label"><span>04</span><b>Context to confirm</b></div><h2>Keep the context <em>specific.</em></h2><p>{article.contextNote}</p></section><section className="related-section"><div className="body-label"><span>05</span><b>Continue researching</b></div><div className="related-grid">{article.related.map((item) => <Link href={item.path} key={item.path}><span>{item.title}</span><ArrowUpRight size={17} /></Link>)}</div></section><section id="sources" className="source-section"><div className="body-label"><span>06</span><b>References</b></div><h2>Official product sources.</h2><ol>{article.references.map((reference, index) => <li key={reference.url}><a href={reference.url} target="_blank" rel="noreferrer">[{index + 1}] {reference.label} <ExternalLink size={13} /></a></li>)}</ol><p className="source-note">Product features, availability, plan scope and pricing can change. Follow the relevant official link before relying on a claim or making a purchase decision.</p></section></article></section></main><SiteFooter /></div>;
}
